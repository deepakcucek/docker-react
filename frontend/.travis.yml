sudo: required
services:
  - docker
before_install:
  - docker build -t deepakcucek/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true deepakcucek/docker-react npm run test -- --coverage
